{
  "name": "forum-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n docker,studio,domain,api -c yellow,blue,red,green \"sh -c 'cd apps/api && docker compose up'\" \"sh -c 'wait-on tcp:5432 && pnpm --filter @forum/api studio'\" \"sh -c 'wait-on tcp:5432 && pnpm --filter @forum/domain dev'\" \"sh -c 'wait-on tcp:5432 && pnpm --filter @forum/api start:dev'\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.1"
  }
}